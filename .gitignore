node_modules/
.env
dist/
.DS_Store
*.log
backend/uploads/*
!backend/uploads/.gitkeep
!backend/uploads/productos/

# Doc con credenciales de la integración Bancard — NO debe subirse al repo
docs/bancard-integracion.md

# Tarjetas físicas: QR + códigos generados para imprimir — NO subir al repo
tarjetas-qr/

# Snapshots automáticos del navegador Playwright MCP (sesiones de prueba)
.playwright-mcp/

# Material de prueba de QR Bancard y evidencias — NO subir al repo
gen-qr-prueba.js
qr-prueba/
bancard-evidencia-*.png

# Config local de Claude Code (permisos/env por máquina) — NO subir al repo
.claude/settings.local.json
